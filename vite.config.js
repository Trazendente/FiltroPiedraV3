import { defineConfig } from 'vite';

export default defineConfig({
  base: '/FiltroPiedraV3/', // Nombre de tu repositorio
  build: {
    outDir: 'docs', // Mantén la carpeta de salida como "docs"
  },
});
