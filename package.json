{
  "name": "filtropiedrav3",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && if exist docs rmdir /s /q docs && move dist docs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^6.0.5"
  },
  "dependencies": {
    "@snap/camera-kit": "^1.1.0"
  }
}
